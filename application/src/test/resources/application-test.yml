spring:
  application:
    name: "@project.name@"
  batch:
    job:
      enabled: false
  datasource:
    url: jdbc:mysql://localhost:3307/worldofbooks
    username: root
    password: password

endpoint:
  name: mockarooendpoint
  url: http://localhost:9561